#!/bin/sh

if insomnia-ischan; then
	insomnia-rev | sed -n '
/^[0-9][0-9]* \[.*\] topic for ..* is: ..*/ {
	s/^[0-9][0-9]* \[.*\] topic for ..* is: \(.*\)/\1/p
	q
}'
else
	name="$(insomnia-name)"
	echo "[(insomnia)]${name:+ $name}"
fi
