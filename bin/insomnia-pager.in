#!/bin/sh
set -e

if [ $# -ne 1 ]; then
	echo "USAGE: ${0##*/} OUTFILE" 1>&2
	exit 1
fi

export PATH="${PATH}:@LIBDIR@"

insomnia-color < "${1}" | \
	tr -d "\006\007" | \
	insomnia-strftime | \
	"${PAGER:-less}"
